"use client"; import {useState} from "react"; import {createClient} from "@supabase/supabase-js"; import Link from "next/link"; const getSupabase=()=>createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY); export default function Login(){const[email,setEmail]=useState(""),[msg,setMsg]=useState("");async function submit(e){e.preventDefault();setMsg("Sending sign-in link…");const{error}=await getSupabase().auth.signInWithOtp({email,options:{emailRedirectTo:window.location.origin+"/dashboard"}});setMsg(error?error.message:"Check your email for the secure sign-in link.");}return <main className="auth"><Link href="/" className="brand">Point<span>Pilot</span></Link><div className="authCard"><div className="eyebrow">SECURE LOGIN</div><h1>Open your PointPilot wallet</h1><p>Use your email. We’ll send a one-time sign-in link.</p><form onSubmit={submit}><input required type="email" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)}/><button className="btn primary" type="submit">Send sign-in link</button></form>{msg&&<div className="notice">{msg}</div>}</div></main>}